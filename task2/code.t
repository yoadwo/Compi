/* long
comment long
comment */

boolean func1(integer a, integer d, integer y, char c)
{
	char x, b;
	boolean fr;

	b = '&';
	a = x;  
	// b = 8; 
	a = (y * 7)/a-y;
	// a = (y * 7)/b-y; 
	// a = (y * 7)/a-c; 

	fr = (b == c) && (y > a);
	// fr = (b == c) && (y + a); 
	// 3 + 6 = 9; 
	// ^x = 6; 
	
    	return fr ;
}


integer func2(integer i, integer j, integer k){
    char x, k;
    boolean func3(integer l, integer m, integer n){    
	boolean x, j, n;
	k = 'k';
	i = l + l;
	// i = j + 1;
	// i = k + 1; 
	if ((k == '*') || (x != false) && (l+m < i)){
		x = l < m;
	}
	//return m;
        return x;
    }
    {
	boolean k;
	k = func3(5,i,j);
	// x = func3(5,i,j); 
	// k = func3(5,i); 
	// k = func3(5,x,j); 
     }
    // n = false; 
    x = '@';
    //return k;
    return 83;
}


integer func4(integer i, integer j, integer k)
{
    integer mult(integer t) /* procedure declarations */ 
    {
        integer temp;
        temp = t* t;
        return temp;
    }

    integer total;                       /* variable declarations */
    boolean bool;
    bool = func1(i,j,k,'^');
    // j = func5(); 
    total = mult(i+j+k);                    /* statements */
    return 0;
}


integer func5() 
string s1[150], s2[150];
    integer i, j,fn5;
    i=0;
    j=0;
    fn5= 1;
    while(i<|s1|){
	while(j<|s2|/2){
	    if (s1[i] == s2[j]){
		fn5 = fn5*2;
	    }
	    j=j+1;
	}
	i=i+1;
    }
    return fn5;
}


void func6()
{
    {   
	integer x;
	integerp y;
	x = 5;
	y = &x;
	x = 6;
	//^x = 7;
	//&x = y;
	//y = x;
	if (&x == y && ^y == x){
		^y = 9;
	}
     	{
		charp x;
		string y[10];
		char z;
		y = "foobar";
		//y='b';
		x = &y[5];
		//x=&y; 
		z = ^x;        
		//z = ^x - 5;       
		y = "barfoo"; 
   		 {
			char x;
			integerp y;
			charp z;
			char g;

			x = ^z; 
			// f = x;
			// x = &(1+3);
			//y = &x;             
			//z = &(&g);
    		  }
		 //z = x;
		 // z = &x;
	}
     }
}



void main()
{
    integer p;
    p = func5();
}