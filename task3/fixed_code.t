
void main()
{
    integer p;
    p = func5();
    boolean func1(integer a, integer d, integer y, char c)
    {
            char x, b;
            boolean fr;

            b = '&';
            // a = x;  
            // b = 8; 
            a = (y * 7)/a-y;
            // a = (y * 7)/b-y; 
            // a = (y * 7)/a-c; 

            fr = (b == c) && (y > a);
            // fr = (b == c) && (y + a); 
            // 3 + 6 = 9; 
            // ^x = 6; 
            
            return fr ;
    }
    
    integer func2(integer i, integer j, integer k)
    {
        char x, k;
        boolean func3(integer l, integer m, integer n)
        {
            boolean x, j, n;
            k = 'k';
            i = l + l;
            // i = j + 1;
            // i = k + 1; 
            if ((k == '*') || (x != false) && (l+m < i)){
                    x = l < m;
            }
            //return m;
            return x;
        }
        {
            boolean k;
            k = func3(5,i,j);
            // x = func3(5,i,j); 
            // k = func3(5,i); 
            // k = func3(5,x,j); 
        }
        // n = false; 
        x = '@';
        //return k;
        return 83;
    }

    
    
    return;
}